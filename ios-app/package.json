{
  "name": "neues-spiel-ios",
  "version": "0.0.1",
  "private": true,
  "description": "Native iOS-Hülle (Capacitor) für Neues Spiel. Bewusst vom abhängigkeitsfreien Kern getrennt.",
  "scripts": {
    "ios": "npm run sync && cap open ios",
    "sync": "cap sync ios && bash scripts/nativ-einrichten.sh",
    "buendeln": "bash scripts/buendeln.sh"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0",
    "@capacitor/push-notifications": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.0",
    "@capacitor/cli": "^7.0.0"
  }
}
