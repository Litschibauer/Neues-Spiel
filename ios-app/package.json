{
  "name": "neues-spiel-ios",
  "version": "0.0.1",
  "private": true,
  "description": "Native iOS-Hülle (Capacitor) für Neues Spiel. Bewusst vom abhängigkeitsfreien Kern getrennt.",
  "scripts": {
    "ios": "cap sync ios && cap open ios",
    "sync": "cap sync ios"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
